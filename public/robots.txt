# dampscan.co.uk
#
# One group only, deliberately. Adding a named group for a crawler makes that
# crawler ignore this one entirely, so the disallows below would stop applying
# to it. The AI crawlers (GPTBot, ClaudeBot, PerplexityBot, Google-Extended and
# the rest) are allowed on purpose: being readable by them is the point.
#
# The London host gets robots-london.txt at this path, via middleware.js.

User-agent: *
Allow: /
Disallow: /api/
Disallow: /staff
Disallow: /staff/
# The area pages are served at /damp-survey/<area>. /areas/ is where the
# files happen to sit, and indexing both would be the same page twice.
Disallow: /areas/

Sitemap: https://dampscan.co.uk/sitemap.xml
