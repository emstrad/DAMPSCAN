# atidampsurvey.co.uk
#
# Served at /robots.txt on the London host by middleware.js. One group only:
# a named crawler group would override this one rather than add to it. The AI
# crawlers are allowed on purpose.

User-agent: *
Allow: /
Disallow: /api/
Disallow: /staff
Disallow: /staff/
# The area pages are served at /damp-survey/<area>. /areas/ is where the
# files happen to sit, and indexing both would be the same page twice.
Disallow: /areas/
# Same for the service pages, which are served at /services/<name>.
Disallow: /service-pages/

Sitemap: https://atidampsurvey.co.uk/sitemap.xml
